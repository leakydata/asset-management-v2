[project]
name = "cat-asset-management-mcp"
version = "0.1.0"
description = "MCP server for the Caterpillar Digital Asset Management V2 API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
